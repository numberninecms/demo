{% include 'partials/countdown_banner.html.twig' %}

<div class="flex flex-row items-center bg-primary text-white px-3 py-2 shadow-lg space-x-4 z-10">
    {% if title is defined %}
        <div class="font-semibold text-xl">{{ title }}</div>
    {% endif %}
    {% block tools %}{% endblock %}
</div>
